org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
kotlin.incremental=false
kotlin.incremental.js=false
# Enable R8 full mode for better code shrinking and optimization
android.enableR8.fullMode=true
# Legacy Kotlin mode until all plugins support AGP 9 built-in Kotlin.
# Removes build failures; share_plus/package_info_plus are updated.
# Remaining KGP warning plugins: screen_protector, sentry_flutter (safe for now).
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
